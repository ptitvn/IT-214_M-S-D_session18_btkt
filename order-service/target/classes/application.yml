# Cấu hình cốt lõi để service biết cách tìm tới Config Server (Câu 1 + Câu 5).
# Toàn bộ cấu hình nghiệp vụ (datasource, jpa, kafka group-id, resilience4j, feign...) nằm ở
# config-repo/order-service.yml và config-repo/application.yml, do Config Server phân phát.
server:
  port: 8081

spring:
  application:
    name: order-service
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      fail-fast: false
      retry:
        max-attempts: 3

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
